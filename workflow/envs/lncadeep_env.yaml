name: lncadeep_env
channels:
    - conda-forge
    - bioconda
    - defaults
dependencies:
    - python>=2.7.6,<3
    - numpy>=1.8.2
    - hmmer=3.1b2
    # Packages not needed for actual pipeline, but to avoid import errors
    - pandas>=0.18.0
    - theano>=0.8.2
    - keras>=1.2.2
    - h5py>=2.5.0
